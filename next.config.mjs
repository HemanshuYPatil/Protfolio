// next.config.js

// Remove Sentry import and configuration
// import { withSentryConfig } from '@sentry/nextjs';

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig; // Simply export the nextConfig without Sentry
